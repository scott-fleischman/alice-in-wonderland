-- Stub file used to indicate entry point of the command-line application

import qualified Alice -- Our library code

-- Just point to the 'main' function in our library.
-- That way all code (in the library) can be reused by other projects.
main :: IO ()
main = Alice.main
